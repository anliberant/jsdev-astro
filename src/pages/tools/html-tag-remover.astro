---
import BaseLayout from '@/app/layouts/base-layout.astro';
import HtmlTagRemover from '@/features/tools/html/tag-remover/html-tag-remover.astro';

const pageTitle = 'HTML Tag Remover | JSdev Tools';
const pageDescription =
  'Remove HTML tags from your content while preserving text. Configure removal options and convert HTML entities with our free online tool.';

export const prerender = false;
---

<BaseLayout
  title={pageTitle}
  description={pageDescription}>
  <HtmlTagRemover />
</BaseLayout>
