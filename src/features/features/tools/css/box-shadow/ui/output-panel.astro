---
import Button from '@/shared/ui/base/button.astro';
---

<div class='output-panel'>
  <div class='output-header'>
    <h2>Preview & Code</h2>

    <div class='tab-group'>
      <button
        class='tab-button active'
        id='preview-tab'
        data-tab='preview'>
        Preview
      </button>
      <button
        class='tab-button'
        id='css-tab'
        data-tab='css'>
        CSS
      </button>
      <button
        class='tab-button'
        id='html-tab'
        data-tab='html'>
        HTML
      </button>
    </div>
  </div>

  <!-- Preview Panel -->
  <div
    class='tab-panel active'
    id='preview-panel'>
    <div class='preview-container'>
      <div
        class='preview-background'
        id='shadow-preview'>
        <div class='preview-box'> Box Shadow Preview </div>
      </div>

      <div class='preview-info'>
        <div class='info-grid'>
          <div class='info-item'>
            <span class='info-icon'>🎨</span>
            <span class='info-text'>Live preview with customizable shape and colors</span>
          </div>
          <div class='info-item'>
            <span class='info-icon'>📐</span>
            <span class='info-text'>Support for multiple shadows and inset effects</span>
          </div>
          <div class='info-item'>
            <span class='info-icon'>🔧</span>
            <span class='info-text'>Fine-tune offset, blur, spread, and opacity</span>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- CSS Panel -->
  <div
    class='tab-panel hidden'
    id='css-panel'>
    <div class='code-container'>
      <div class='code-header'>
        <span class='code-title'>CSS Code</span>
        <div class='code-actions'>
          <Button
            variant='copy'
            size='sm'
            id='copy-css'>
            Copy CSS
          </Button>
        </div>
      </div>
      <textarea
        id='css-output'
        class='code-output'
        readonly
        placeholder='Generated CSS will appear here...'
      ></textarea>
    </div>
  </div>

  <!-- HTML Panel -->
  <div
    class='tab-panel hidden'
    id='html-panel'>
    <div class='code-container'>
      <div class='code-header'>
        <span class='code-title'>HTML Code</span>
        <div class='code-actions'>
          <Button
            variant='copy'
            size='sm'
            id='copy-html'>
            Copy HTML
          </Button>
          <Button
            variant='default'
            size='sm'
            id='copy-both'>
            Copy Both
          </Button>
        </div>
      </div>
      <textarea
        id='html-output'
        class='code-output'
        readonly
        placeholder='Generated HTML will appear here...'
      ></textarea>
    </div>
  </div>

  <!-- Usage Instructions -->
  <div class='usage-section'>
    <h3>How to Use</h3>
    <ol class='usage-list'>
      <li>Select a preset or create your own shadow configuration</li>
      <li>Add multiple shadows for complex effects</li>
      <li>Adjust position, blur, spread, and opacity for each shadow</li>
      <li>Customize the preview shape and background color</li>
      <li>Copy the generated CSS or HTML code</li>
    </ol>

    <div class='tips'>
      <h4>Tips:</h4>
      <ul class='tips-list'>
        <li>Use multiple subtle shadows for depth and realism</li>
        <li>Inset shadows create pressed or recessed effects</li>
        <li>Lower opacity values create more natural shadows</li>
        <li>Consider using colored shadows for modern designs</li>
        <li>Test shadows on different background colors</li>
      </ul>
    </div>
  </div>
</div>

<style>
  .output-panel {
    background: hsl(var(--background));
    border: 1px solid hsl(var(--border));
    border-radius: var(--radius);
    overflow: hidden;
    display: flex;
    flex-direction: column;
    min-height: 600px;
  }

  .output-header {
    background: hsl(var(--muted));
    border-bottom: 1px solid hsl(var(--border));
    padding: 1rem 1.5rem;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
  }

  .output-header h2 {
    margin: 0;
    font-size: 1.25rem;
    font-weight: 600;
    color: hsl(var(--foreground));
  }

  .tab-group {
    display: flex;
    background: hsl(var(--background));
    border: 1px solid hsl(var(--border));
    border-radius: var(--radius);
    padding: 0.25rem;
  }

  .tab-button {
    padding: 0.5rem 1rem;
    border: none;
    background: transparent;
    border-radius: calc(var(--radius) - 2px);
    font-size: 0.875rem;
    font-weight: 500;
    cursor: pointer;
    transition: all 0.2s ease;
    color: hsl(var(--muted-foreground));
  }

  .tab-button.active {
    background: hsl(var(--primary));
    color: hsl(var(--primary-foreground));
  }

  .tab-button:hover:not(.active) {
    background: hsl(var(--accent));
    color: hsl(var(--accent-foreground));
  }

  .tab-panel {
    flex: 1;
    display: flex;
    flex-direction: column;
  }

  .tab-panel.hidden {
    display: none;
  }

  .preview-container {
    flex: 1;
    display: flex;
    flex-direction: column;
  }

  .preview-background {
    flex: 1;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    display: flex;
    align-items: center;
    justify-content: center;
    min-height: 400px;
    position: relative;
    padding: 3rem;
  }

  .preview-box {
    width: 200px;
    height: 120px;
    background: #ffffff;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 600;
    color: #374151;
    font-size: 0.875rem;
    text-align: center;
    transition: all 0.3s ease;
  }

  .preview-info {
    padding: 1.5rem;
    background: hsl(var(--muted) / 0.5);
    border-top: 1px solid hsl(var(--border));
  }

  .info-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 1rem;
  }

  .info-item {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    padding: 0.75rem;
    background: hsl(var(--background));
    border: 1px solid hsl(var(--border));
    border-radius: var(--radius);
    transition: all 0.2s ease;
  }

  .info-item:hover {
    transform: translateY(-1px);
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
  }

  .info-icon {
    font-size: 1.25rem;
    flex-shrink: 0;
  }

  .info-text {
    font-size: 0.875rem;
    color: hsl(var(--foreground));
    font-weight: 500;
  }

  .code-container {
    flex: 1;
    display: flex;
    flex-direction: column;
  }

  .code-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 1rem 1.5rem;
    background: hsl(var(--muted) / 0.5);
    border-bottom: 1px solid hsl(var(--border));
  }

  .code-title {
    font-size: 0.875rem;
    font-weight: 500;
    color: hsl(var(--foreground));
  }

  .code-actions {
    display: flex;
    gap: 0.5rem;
  }

  .code-output {
    flex: 1;
    padding: 1.5rem;
    border: none;
    outline: none;
    resize: none;
    background: hsl(var(--background));
    color: hsl(var(--foreground));
    font-family: var(--font-mono);
    font-size: 0.875rem;
    line-height: 1.5;
    min-height: 300px;
  }

  .code-output:focus {
    background: hsl(var(--muted) / 0.3);
  }

  .usage-section {
    padding: 1.5rem;
    background: hsl(var(--muted) / 0.3);
    border-top: 1px solid hsl(var(--border));
  }

  .usage-section h3 {
    margin: 0 0 1rem 0;
    font-size: 1rem;
    font-weight: 600;
    color: hsl(var(--foreground));
  }

  .usage-section h4 {
    margin: 1rem 0 0.5rem 0;
    font-size: 0.875rem;
    font-weight: 600;
    color: hsl(var(--foreground));
  }

  .usage-list,
  .tips-list {
    margin: 0;
    padding-left: 1.5rem;
    color: hsl(var(--muted-foreground));
    font-size: 0.875rem;
    line-height: 1.5;
  }

  .usage-list li,
  .tips-list li {
    margin-bottom: 0.5rem;
  }

  .tips {
    margin-top: 1rem;
    padding-top: 1rem;
    border-top: 1px solid hsl(var(--border));
  }

  /* Copy button states */
  :global(.copied) {
    background: hsl(var(--success)) !important;
    color: white !important;
  }

  @media (max-width: 768px) {
    .output-header {
      flex-direction: column;
      align-items: stretch;
      gap: 1rem;
    }

    .tab-group {
      justify-content: center;
    }

    .code-header {
      flex-direction: column;
      align-items: stretch;
      gap: 1rem;
    }

    .code-actions {
      justify-content: center;
    }

    .usage-section {
      padding: 1rem;
    }

    .preview-background {
      min-height: 300px;
      padding: 2rem;
    }

    .info-grid {
      grid-template-columns: 1fr;
      gap: 0.75rem;
    }

    .preview-box {
      width: 150px;
      height: 100px;
      font-size: 0.75rem;
    }
  }

  :global(.dark) .preview-background {
    background: linear-gradient(135deg, #1e3a8a 0%, #581c87 100%);
  }

  :global(.dark) .preview-box {
    background: #1f2937;
    color: #f3f4f6;
  }
</style>
