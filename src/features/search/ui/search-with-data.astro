---
import Search from './search.astro';

export interface Props {
  placeholder?: string;
  className?: string;
}

const { placeholder = 'Search...', className } = Astro.props;
---

<Search placeholder={placeholder} className={className} />
